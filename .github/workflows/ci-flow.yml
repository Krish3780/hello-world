name: CI deployment
on: push
jobs:
  fetch-code:
    runs-on: ubuntu-latest
    steps:
      - name: get the code from repo
        uses: actions/checkout@v5
      - name: list the files    
        run: |
          ls -l
          pwd